{
  "name": "@webmcp-cloud-relay/extension",
  "version": "0.1.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "node scripts/gen-icons.mjs && node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@webmcp-cloud-relay/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.7",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
